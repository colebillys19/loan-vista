const moment = require('moment');

const EPOCH_MOMENT = moment('1970-01-01T00:00:00.000Z');

const MOCK_DATA_DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';

module.exports = { EPOCH_MOMENT, MOCK_DATA_DATE_FORMAT };
