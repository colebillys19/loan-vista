/**
 * CorrList
 * @description ...
 */

import React from 'react';
// import T from 'prop-types';

const CorrList = () => <div>CorrList</div>;

CorrList.propTypes = {};

export default CorrList;

/*
<StyledTable>
  <StyledTableHead>
    <tr>
      {headers.map((header) => (
        <StyledTableHeader key={header} scope="col">{header}</StyledTableHeader>
      ))}
    </tr>
  </StyledTableHead>
  <tbody>
    {corrData.map((rowData, i) => {
      const { id } = rowData;
      return <TableRow data={rowData} headers={headers} key={id} />;
    })}
  </tbody>
</StyledTable>
*/
