/**
 * CallsList
 * @description ...
 */

import React from 'react';
// import T from 'prop-types';

const CallsList = () => <div>CallsList</div>;

CallsList.propTypes = {};

export default CallsList;

/*
<StyledTable>
  <StyledTableHead>
    <tr>
      {headers.map((header) => (
        <StyledTableHeader key={header} scope="col">{header}</StyledTableHeader>
      ))}
    </tr>
  </StyledTableHead>
  <tbody>
    {callsData.map((rowData, i) => {
      const { id } = rowData;
      return <TableRow data={rowData} headers={headers} key={id} />;
    })}
  </tbody>
</StyledTable>
*/
